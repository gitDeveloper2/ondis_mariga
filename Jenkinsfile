pipeline {
    agent { 
       
            dockerfile true
            
        
        
    }
      stages {
        stage('After Docker') {
      steps {
        sh 'python --version'
      }
    }
  }
}
